// Test stub for `$app/environment`. jsdom is a browser-like environment, so we
// report `browser = true` — this exercises the localStorage / navigator / toast
// branches that only run client-side.
export const browser = true;
export const building = false;
export const dev = true;
export const version = 'test';
